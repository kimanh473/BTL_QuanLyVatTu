@model List<Export>
@{
    ViewData["Title"] = "QLVT - Lịch sử xuất kho";
    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    </head>
    <body>
        <div class="container-sm mt-5">
            <h4>Lịch sử xuất kho</h4>
            <table class="table table-striped mb-5">
                <thead class="table-dark">
                    <tr>
                        <th>Mã chứng từ</th>
                        <th>Ngày xuất</th>
                        <th>Mã vật tư</th>
                        <th>Tên vật tư</th>
                        <th>Loại hàng</th>
                        <th>Số lượng</th>
                        <th>Đơn vị tính</th>
                        <th>Đơn giá</th>
                        <th>Thành tiền</th>
                        <th>Người đề nghị</th>
                        <th>Người nhận</th>
                        <th>Ghi chú</th>                      
                    </tr>
                </thead>
                <tbody>
                    @foreach (var export in Model)
                    {
                        <tr>
                            <td>@export.doc_id</td>
                            <td>@export.export_date.ToString("MM/dd/yyyy")</td>
                            <td>@export.product_id</td>
                            <td>@export.product_name</td>
                            <td>@export.product_type</td>
                            <td>@export.quantity.ToString("N0")</td>
                            <td>@export.currency</td>
                            <td>@export.export_price.ToString("N0") VND</td>
                            <td>@export.total.ToString("N0") VND</td>
                            <td>@export.promoter</td>
                            <td>@export.receiver</td>
                            <td>@export.note</td>
                        </tr>
                    }

                </tbody>
            </table>
        </div>
    </body>
}
